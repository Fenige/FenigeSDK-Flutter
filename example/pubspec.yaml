name: example
description: "Fenige SDK example"

# Prevent accidental publication to pub.dev
publish_to: 'none'

# Application version
version: 1.0.0+1

environment:
  sdk: '>=3.2.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  fenige_sdk:
    path: ../
  cupertino_icons: ^1.0.2
  uuid: ^3.0.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true